# FFmpeg MCP Server Configuration

# 传输方式: stdio (标准输入输出) 或 sse (HTTP Server-Sent Events)
MCP_TRANSPORT=sse

# 服务器监听地址 (仅在 sse 模式下有效)
MCP_HOST=0.0.0.0

# 服务器监听端口 (仅在 sse 模式下有效)
MCP_PORT=8032

# 对外公开访问的基准 URL (可选)，例如 http://your-public-ip:8032
MCP_EXTERNAL_URL=http://localhost:8032

## 安全设置 (针对较新版本 SDK)
# 允许的 Host 头校验 (逗号隔开，支持通配符)
MCP_ALLOWED_HOSTS=localhost:*,127.0.0.1:*,ffmpeg-mcp-server:*
# 是否开启 DNS 重绑定保护 (建议生产环境开启)
MCP_ENABLE_DNS_REBINDING_PROTECTION=false
# MCP_EXTERNAL_URL=http://ffmpeg-mcp-server:8032

# Python 输出缓冲
PYTHONUNBUFFERED=1
